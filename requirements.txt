# Spleeter and its dependencies
spleeter==2.4.0
tensorflow==2.9.0  # Compatible with Spleeter 2.4.0
numpy==1.23.4  # Ensure this version of NumPy is compatible
ffmpeg-python==0.2.0  # Optional: If you're processing audio files with ffmpeg
Flask # Or the version you want
werkzeug==2.0.3  # Add this line to fix the compatibility issue

# Additional dependencies
librosa==0.9.1  # Optional depending on use case
scipy==1.10.0    # For signal processing (optional)
